import { DiscordIcon, GitHubIcon, GoogleIcon, USDCIcon, XIcon } from './icons';

// -----------------------------------------------------------------------------
// Export
// -----------------------------------------------------------------------------

const LogoIcon = () => null;

LogoIcon.Discord = DiscordIcon;
LogoIcon.GitHub = GitHubIcon;
LogoIcon.Google = GoogleIcon;
LogoIcon.USDC = USDCIcon;
LogoIcon.X = XIcon;

LogoIcon.displayName = 'LogoIcon';

export default LogoIcon;
